  for(int i = 0 ; i < n ; i++){
                char curr = s.charAt(i); 
                if(st.isEmpty()){
                    st.push(curr); 
                }else if(st.peek() == 'b' && curr == 'a'){
                    ans += y ; 
                    st.pop(); 
                }else{
                    st.push(curr);
                }
            }
            Stack<Character> st2 = new Stack<>(); 
            while(!st.isEmpty()){
                char curr = st.pop(); 
                if(!st2.isEmpty() && curr == 'a' && st2.peek() == 'b'){
                    ans += x ; 
                    st2.pop(); 
                }else{
                    st2.push(curr); // thi becomes the prev 
                }
            }
        }
        
        return ans; 
    }
}