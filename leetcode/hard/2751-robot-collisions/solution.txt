t<>();

        while (!st.isEmpty()) {
            int curr[] = st.pop();
            list.add(new int[]{curr[0], curr[2]});
        }

        Collections.sort(list, (a, b) -> a[0] - b[0]);

        List<Integer> ans = new ArrayList<>();
        for (int i = 0; i < list.size(); i++) {
            int temp = list.get(i)[1];
            ans.add(temp);
        }

        return ans;
    }
}