# Copy to extension/.env only when creating the distributable release.
# This must be the public HTTPS URL of the deployed backend, not localhost.
PLASMO_PUBLIC_BACKEND_URL=https://api.example.com
PLASMO_PUBLIC_GITHUB_CLIENT_ID=your-github-oauth-client-id
